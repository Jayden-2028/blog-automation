# writer.md §6–10 — 출력 형식 계약

> `prompts/writing/writer.md`에서 분리된 파일이다(2026-09-15, 구조 분리 리팩터). writer.md §1이
> 이 파일을 필수 로딩 목록에 넣어 항상 함께 읽는다.
>
> ⚠️ **이 파일은 코드와 직접 맞물려 있다.** 소제목 서식(볼드 한 줄)은
> `convertArticleToHtml.ts`/`convertArticleToNaverHtml.ts`/`markdownToTelegraphNodes.ts`/
> `parseManuscriptBlocks.ts`가 그대로 파싱 기준으로 쓰고, 이미지 마커 규칙은
> `generateManuscriptImages.ts`의 삽입 지점 판정과 맞물린다. **문체가 아니라 계약이다** - 여기를
> 고치면 반드시 그 코드들도 같이 확인한다(§7/§8 각 절에 관련 코드 경로를 적어뒀다).

## 6. 규칙 충돌 해소

세 문서(스킬 문체, seo-guide, 이 계약)를 동시에 지키면 아래 지점에서 부딪힌다. **정해진 답으로만 처리한다.**

### 충돌 1 — 오프닝: 개인 에피소드 vs AEO 직답

seo-guide는 첫 문장이 인사말 없는 직답이어야 한다고 하고, 세 스킬은 전부 개인적 상황 묘사로 연다.

**해소:** 에피소드로 열되, **오프닝 직후 문단에서 바로 핵심 답을 준다.** parenting 스킬 흐름 4번이 원래 이 구조라 억지가 아니다. 엔터·트렌드도 동일하게 오프닝 → 핵심 소식 한 문장 순서로 배치한다.

**"결론부터 말씀드리면"을 매번 기계적으로 붙이지 않는다.** 이 문구는 실제 발행본에서도 정책·제도 안내 글처럼 "안심시키는 요지"가 필요할 때 가끔 자연스럽게 등장할 뿐, 엔터테인먼트·트렌드 글의 오프닝에서는 거의 쓰이지 않는다. 그 문단 자체가 이미 답이라는 걸 문장 내용으로 보여주면 충분하다 — 앞에 "이게 결론이다"라고 예고하는 메타 문구는 군더더기다.

```
지난주에 지우가 새벽마다 깨서 한동안 잠을 설쳤습니다.     ← 에피소드 오프닝
사실 이런 고민, 저만 하는 게 아니더라고요.                ← 공감

밤중수유는 생후 6개월 이후부터 끊는 걸 고려할 수           ← AEO 직답(인용 단위).
있다고 해요.                                                문구 없이 바로 답으로 들어간다

(정책·안내형 글이라 자연스러운 경우에만, 선택적으로)
결론부터 말씀드리면, 이번 지원금은 8월 27일 지급이         ← 이런 글에서는 자연스럽지만
목표입니다.                                                 "매번" 쓰는 규칙은 아니다
```

### 충돌 2 — 소제목: 마크다운 헤더를 쓸 것인가

parenting은 헤더를 안 쓰고, trend는 `###`를 쓰고, seo-guide는 네이버 에디터에 H태그가 없다고 말한다.
(2026-09-06 갱신: 이전엔 "평문 한 줄"로 정했지만 실제 발행 경로(HTML 변환기)가 소제목을 시각적으로
구분해야 해서 `##` 헤더가 쓰였고, 그마저 카테고리마다 들쭉날쭉했다. 이제 **볼드 한 줄**로 명확히
통일한다 - `#`처럼 그대로 붙여넣었을 때 기호가 보이는 문제 없이, 변환기가 파싱할 명확한 신호도 된다.)

**해소: `#` 기호 대신 `**소제목**` 볼드 한 줄을 쓴다.** 규칙:

- 소제목 **앞에는 빈 줄 1개**로 이전 블록과 띄운다.
- 소제목 **바로 다음 줄에는 빈 줄을 넣지 않는다** — 그 소제목에 딸린 첫 문단이 줄바꿈 하나만 두고 바로 붙는다(소제목과 그 문단은 한 덩어리).
- 소제목 텍스트 자체에는 `**`를 넣지 않는다(줄 전체를 감싸는 `**...**` 한 쌍뿐).

```
(X) ## 밤중수유는 언제부터 끊나요

(X)
밤중수유는 언제부터 끊나요

26개월 전후로...

(O)
**밤중수유는 언제부터 끊나요**
26개월 전후로...
```

- 소제목 문구는 평서문이나 질문형으로 짓는다: "26개월 전후, 실제 평균은 이 정도예요"
- 명사형 소제목("기본 정보", "캐스팅 소개")은 금지.
- 제목 줄에는 `#`도 `**`도 쓰지 않는다. 제목은 frontmatter의 `title`에 넣고, 본문은 오프닝부터 시작한다.

**마무리 문단에는 소제목을 달지 않는다.** "결론", "마무리하며", "제 생각은요", "정리하자면" 같은 줄을 넣지 않고, 마지막 섹션에서 자연스럽게 마무리 문단으로 넘어간다.

### 충돌 3 — 해시태그 개수

엔터·트렌드 스킬은 정확히 10개, seo-guide는 5~10개다.

**해소:** 10개로 맞춘다. 범위 안이라 충돌이 아니다. parenting은 스킬상 선택 사항이지만 SEO를 위해 **항상 넣는다**(5~10개). 모든 태그는 본문에 실제로 나온 주제여야 하고, 글마다 같은 세트를 복사하지 않는다.

### 충돌 4 — 키워드 반복 vs 자연스러운 말투

seo-guide는 메인 키워드 3~5회를 권하고, 스킬 문체는 같은 말 반복을 싫어한다.

**해소:** 키워드는 **제목 / 첫 문단 직답 / 소제목 1~2개 / 마무리**에만 원형으로 넣고, 본문 나머지는 대명사와 유의어로 푼다. 5회를 넘기지 않는다. 10회 이상은 스터핑이고 AI 검색에서는 역효과다.

### 충돌 5 — 분량 vs 담백한 문체

seo-guide는 1,500자를 하한으로 두지만, **이 파이프라인의 실제 목표는 공백 제외 2,000~3,000자**다. 스킬 문체는 짧고 담백한 문장을 쓴다.

**해소:** 문장을 늘리지 말고 **섹션을 늘린다.** 리서치의 `사람들이 실제로 묻는 질문`을 Q&A 섹션으로, `수치·기준 정리`를 목록으로 옮기면 분량과 AEO를 동시에 채운다. 같은 내용을 다시 말해 분량을 채우는 것은 금지다.

- **2,000자 미만**: 리서치가 얇은 것이다. 늘려 쓰지 말고 자료조사 보강을 요청한다.
- **3,000자 초과**: 주제가 두 개 섞여 있는 것이다. 세부 주제로 쪼개 두 편으로 만든다(스마트블록 대응에도 유리하다).

---

## 7. 본문 구조

스킬의 흐름을 뼈대로 삼고, seo-guide 요소를 그 위에 얹는다.

```
[오프닝]  핵심 소재 제시 → (자연스러우면) 개인적 맥락·공감    (강제 공식 아님 - 2026-09-06)
[직답]    핵심 답을 바로 제시하는 문단           (AEO — "결론부터 말씀드리면"을 매번 쓰지 않는다)


[IMAGE: ...]
[IMAGE PROMPT: ...]


**소제목 1 — 독자의 실제 질문 형태**            ← 볼드 한 줄, 바로 다음 줄에 문단이 붙는다(빈 줄 없음)
결론 먼저 → 근거(등급별 서술) → 경험/예시. 문단 3~5줄.


[IMAGE: ...]
[IMAGE PROMPT: ...]


**소제목 2**
표 또는 번호 목록 최소 1개                    (GEO)

**소제목 3~5**


[IMAGE: ...]
[IMAGE PROMPT: ...]


**자주 묻는 질문**
Q. / A. 3~5개, 답변 각 2~3문장               (AEO)

(소제목 없이 바로 마무리 문단)
  따옴표 한 문장 메시지 + 응원/댓글 유도         (스킬 흐름)

#해시태그 10개
```

빈 줄 개수가 곧 규칙이다: **소제목→문단은 0줄**(붙여 쓴다), **문단→문단·소제목→소제목은 1줄**,
**이미지 마커 쌍 앞뒤는 2줄**(위 다이어그램처럼 위아래로 한 줄 더 띄운다). 이렇게 해야 그대로
복사해 붙여넣었을 때도 이미지가 들어갈 자리가 시각적으로 도드라진다.

- 소제목 총 **2~7개**. 전부 `**볼드**` 한 줄이고, 앞에만 빈 줄로 띄운다(뒤는 붙인다).
- **마무리에는 소제목을 달지 않는다.** 마지막 섹션에서 그대로 이어진다.
- 각 섹션은 떼어놔도 말이 되게 쓴다(AI 인용 단위).
- 문단은 3~5줄. 모바일에서 한 덩어리로 읽히는 크기.
- 대명사로 문단을 시작하지 않는다 — "이건", "위에서 말한" 대신 주어를 다시 쓴다.
- 본문 어딘가에 **확인 시점**을 남긴다: "2026년 8월 기준으로 찾아본 내용이에요".

### 표 대신 글머리 목록 — 마크다운 표(`|`)를 쓰지 않는다

**마크다운 표(`| 칸 | 칸 |` 형태)는 쓰지 않는다(2026-09-06 변경).** 표는 GEO에 유리하지만, `|` 기호가
그대로 남아 각 블로그 에디터에 붙여넣을 때 번거롭다. 같은 정보는 소제목 + 글머리 목록으로 푼다.

- 항목이 여러 개면 소제목 하나를 달고, 그 아래 `- **항목명**: 값` 형태로 한 줄씩 나열한다.
- 값이 길어지는 항목은 콜론 뒤에 그대로 이어 쓰거나, 필요하면 그 항목만 별도 문단으로 뺀다.
- 항목은 3~8개가 읽기 좋다. 그보다 많으면 핵심만 남기고 나머지는 버린다.
- 비교축이 2개 이상(예: 연도별 × 항목별)이라 목록으로 못 풀 만큼 복잡하면, 표를 억지로 쓰지 말고
  가장 중요한 축 하나만 골라 목록으로 단순화한다.

```
(X) | 구분 | 지원 대상 | 지원 금액 | 신청 기간 |
    |------|--------|--------|--------|
    | 1차  | 전 국민 | 25만 원 | 정부24 |

(O) **1차 지원 내용**
    - 대상: 전 국민
    - 금액: 25만 원
    - 신청: 정부24
```

---

## 8. 이미지 마커

> 관련 코드: `generateManuscriptImages.ts`(삽입 지점 판정), `parseDraftFile.ts`(마커 파싱).

이미지는 자동 생성하지 않는다. **writer가 프롬프트까지 써주고, 사용자는 그 프롬프트를 그대로 복사해 AI 생성 도구나 이미지 검색창에 붙여넣어 이미지를 직접 구한다.**
따라서 한 자리마다 **마커 두 줄을 붙여서** 넣는다.

```
[IMAGE: 사람이 읽는 설명 — 이 자리에 무엇이 들어가는가]
[IMAGE PROMPT: 그대로 복사해서 쓸 수 있는 프롬프트 — AI 생성 도구 또는 이미지 검색창에 붙여넣기]
```

두 줄은 **반드시 붙여 쓰고**, 그 묶음의 앞뒤를 **빈 줄 2개**로 띄운다(다른 블록 사이의 빈 줄 1개보다 넓게 - §7 참고, 이미지 자리를 시각적으로 도드라지게 한다). 설명만 있고 프롬프트가 없거나, 그 반대인 마커는 미완성이다. **`IMAGE PROMPT` 줄은 예외 없이 실제로 붙여넣어 쓸 수 있는 문자열이어야 한다** — "생성 안 함", "○○ 찾아서 캡처" 같은 지시문만 적힌 마커는 미완성으로 친다. 사용자가 그 자리에서 바로 붙여넣기만 하면 되게 쓴다.

> 두 번째 줄을 `IMAGE`가 아니라 `IMAGE PROMPT`로 구분하는 이유: 같은 이름이면 후속 스크립트가 설명과 프롬프트를 구분하지 못하고, 사용자도 어느 줄을 복사해야 할지 헷갈린다.

### 개수와 위치

- **최소 5쌍.** seo-guide 기준(이미지 5장 이상)에 맞춘다.
- 한곳에 몰지 않고 **섹션 전환 지점마다** 나눠 배치한다(엔터 스킬 패턴).
- 표준 배치: 오프닝 직후 1개 → 각 소제목 사이 1개씩 → 마무리 직전 1개.
- 3~5줄 텍스트마다 1장 리듬을 넘지 않게 한다(너무 촘촘해도 읽기 나쁘다).

### 8-1. 프롬프트는 "바로 위 문단을 한 장으로" 요약한다 (2026-09-16)

이미지 마커는 **바로 위 문단(또는 그 소제목 섹션)에 속한다.** 프롬프트는 그 문단이 말하는 것을
**한 장면·한 장의 카드로 요약**한 것이어야 한다 — "이 문단을 그림 한 장으로 그리면"이지, 글의
분위기에 어울리는 컷이 아니다. 실측(2026-09-16, 아시안게임 야구 원고): "토너먼트 그래픽" 프롬프트로
만든 이미지에 어느 팀이 어디서 붙는지가 하나도 없어 본문과 따로 놀았다 - 문단은 4개국 대진을
설명하는데 이미지는 아무 대진표였다.

쓰는 순서:

1. 그 문단의 핵심을 **한국어 한 문장**으로 적는다. 이 문장이 첫 줄 설명이 된다.
   `[IMAGE: 한국·일본·대만·호주가 두 조로 나뉘어 8강에서 만나는 대진 구조 — 웹 검색]`
2. 그 문장의 **구체 요소**(누가·무엇·몇 개·어떤 순서·어떤 구조)를 둘째 줄 프롬프트의 "대상 → 동작·구성"
   요소로 옮긴다. 문단의 구체 요소가 프롬프트에 **최소 2개** 들어가야 한다.
3. 문단을 안 읽은 사람이 프롬프트만 보고 그 문단 내용을 짐작할 수 있는지 확인한다. 못 하면 분위기 컷이다.

```
(X) [IMAGE: 토너먼트 분위기의 그래픽 — AI 생성]
    [IMAGE PROMPT: A dynamic sports tournament bracket graphic, energetic, no text, 16:9]
      → 문단(4개국 조 편성)의 요소가 하나도 없다. 어떤 문단에 붙여도 되는 이미지 = 어느 문단에도 안 맞는 이미지.

(O) [IMAGE: 한국·일본·대만·호주가 두 조로 나뉘어 8강에서 만나는 대진 구조 — 웹 검색]
    [IMAGE PROMPT: 2026 아시안게임 야구 대진표 조 편성 공식]
      → 팀명·조 구조가 핵심이라 글자가 필요하다 = AI 생성이 아니라 웹 검색.

(O) [IMAGE: 저녁 7시 목욕 → 불 끄기 → 그림책 → 취침, 저희 집 4단계 루틴 — AI 생성]
    [IMAGE PROMPT: A warm flat illustration of a Korean family's four-step bedtime routine, left to right:
    a toddler in a bathtub at 7 pm, a parent dimming a bedside lamp, twins listening to a picture book,
    two children asleep in one room. Soft beige and muted blue, no text, no letters, 16:9.]
      → 문단의 요소(4단계·순서·쌍둥이)가 그대로 들어 있다.
```

**글자·데이터가 핵심인 문단은 AI 생성으로 만들지 않는다.** 대진표, 일정표, 순위·순서, 지역별 금액,
비교표, 그래프처럼 **정보가 글자로 전달되는 이미지**는 `no text` 규칙 때문에 정보가 통째로 사라지고
"아무 표" 이미지가 된다. 이런 자리는 첫 줄을 `웹 검색`으로 지정하고 둘째 줄에 공식 자료를 특정하는
검색어(기관·대회·연도·자료명)를 쓴다. 검수 코드(`checkImagePrompts`)가 `AI 생성` 설명에 대진표·일정표·
표·그래프류 단어가 있으면 경고한다.

### 8-2. 화면 캡처는 이미지 자리로 쓰지 않는다 (2026-09-17)

**법령 조문 화면, 정부·공공 포털 화면, 기관 홈페이지 화면, 앱·웹사이트 화면 캡처를 이미지 마커로
만들지 않는다.** 획득 방식이 `AI 생성`이든 `웹 검색`이든 마찬가지다.

왜(사용자 결정 + 실측 3가지):

1. **웹에 이미지 파일로 존재하지 않는다.** 법령 포털의 조문은 HTML 페이지이지 `.jpg`가 아니다.
   자동 수집(`npm run images:collect`)이 이 자리를 채우지 못하고 매번 빈 자리로 남는다 - 09-16
   남양주 카페 원고의 "국가법령정보센터 제41조 화면" 자리가 실제로 이 사유로 비었다.
2. **글자 이미지라 유입에 불리하다.** 구글 디스커버는 글자가 많은 이미지를 썸네일로 잘 고르지
   않는다(§8-1과 같은 근거, `docs/seo-guide.md`).
3. **독자에게 줄 게 없다.** 조문 텍스트나 화면 안내는 본문 문장·표로 쓰면 그만이다. 그걸 다시
   그림으로 보여줄 이유가 없다.

**대신 무엇을 넣는가 — 그 제도·수치가 "적용되는 현장"의 실사 사진을 찾는다.** 화면이 아니라 사람과
장소를 보여주는 자리로 바꾼다.

| 원래 쓰려던 화면 캡처 | 대신 쓸 이미지(전부 `웹 검색`) |
|---|---|
| 산업안전보건법 제41조 조문 화면 | 카페·콜센터 카운터에서 고객을 응대하는 직원, 계산대에 붙은 "직원에게 폭언을 하지 말아주세요" 안내문 |
| 정부24 지원금 신청 화면 | 주민센터 창구에서 신청하는 시민, 지자체가 배포한 지원금 안내 포스터·현수막 |
| 질병관리청 성장도표 표 화면 | 영유아 검진을 받는 아이, 소아과 진료실 풍경 (수치 자체는 본문 표로) |
| 기상청 특보 화면 | 그 기상 상황의 실제 현장 사진(침수된 도로, 눈 덮인 거리) |
| 방송사 편성표 화면 | 그 프로그램 제작발표회·기자간담회 현장 사진 |

공공기관이 **배포한 사진·캠페인 포스터·보도자료 이미지**는 계속 좋은 소재다(저작권도 안전하다) -
금지하는 것은 **웹사이트를 찍은 화면**이지 공공기관 자료 자체가 아니다.

수치·절차·목록은 이미지로 만들려 하지 말고 본문의 표나 번호 목록으로 쓴다(§7).

### 첫 줄 — 설명 쓰는 법

무엇이 들어가는지 **바로 알 수 있게** 쓴다. 추상적인 지시는 금지다. 설명 끝에 획득 방식을 `—` 뒤에 붙인다.

```
(X) [IMAGE: 육아 관련 이미지]
(X) [IMAGE: 분위기 있는 사진]
(O) [IMAGE: 수면 루틴 4단계를 정리한 일러스트 카드 — AI 생성]
(O) [IMAGE: 영유아 검진에서 키를 재는 아이와 간호사 — 웹 검색]
(O) [IMAGE: 가을장마 종료를 발표한 기상청 브리핑 사진 — 웹 검색]
```

획득 방식은 둘 중 하나로만 적는다: **AI 생성 / 웹 검색**.

- 인물이 나와야 하면 얼굴 노출 여부까지 적는다.
- 웹 검색이면 **무엇을 찾아야 하는지**(기관명·자료명·연도, 인물명·행사명 등)까지 설명에 포함한다 — 둘째 줄 검색어와 짝이 맞아야 한다.

### 둘째 줄 — 프롬프트 쓰는 법

**획득 방식이 `AI 생성`이면 이미지 생성 프롬프트를, `웹 검색`이면 검색어를 쓴다.** 어느 쪽이든 사용자가 그대로 복사해 붙여넣을 수 있는 완성된 문자열이어야 한다 — "○○ 찾아서 캡처"처럼 사람이 할 일을 지시하지 말고, 검색창에 바로 칠 검색어 자체를 쓴다.

```
[IMAGE: 수면 루틴 4단계를 정리한 일러스트 카드 — AI 생성]
[IMAGE PROMPT: A warm minimal flat illustration of a bedtime routine in a Korean home,
four simple scenes left to right: warm bath, dim lamp, picture book, sleeping child.
Soft beige and muted blue palette, rounded shapes, no text, no letters.
Clean white background, gentle evening lighting. 4:3 aspect ratio.]

[IMAGE: 영유아 검진에서 키를 재는 아이와 간호사 — 웹 검색]
[IMAGE PROMPT: 영유아 건강검진 신체계측 키 측정 사진]

[IMAGE: 가을장마 종료를 발표한 기상청 브리핑 사진 — 웹 검색]
[IMAGE PROMPT: 기상청 가을장마 종료 브리핑 2026 사진]
```

**AI 생성 프롬프트 규칙**:

- **기본은 실사 사진이다(2026-09-17 사용자 결정).** 프롬프트에 `photorealistic photograph`와 카메라 느낌을
  주는 표현(`natural lighting`, `shallow depth of field`, `documentary style` 등)을 넣는다. 예전 예시가
  전부 `flat illustration`이었던 탓에 원고 대부분이 일러스트로 나왔다 - seo-guide가 실사를 우대하고,
  독자에게도 실제 상황이 더 잘 전달된다.
  **일러스트는 예외로만 쓴다**: 실물로 찍을 수 없는 개념·절차 도식(단계 흐름, 구조 비교)일 때만.
  그 경우에도 첫 줄 설명에 "일러스트"라고 밝힌다.
  ```
  (X) A warm flat illustration of a piggy bank with coins, soft beige palette. 16:9.
  (O) A photorealistic photograph of a young Korean office worker checking a savings account
      passbook at a bank counter, natural window light, shallow depth of field,
      documentary style, no text, no letters. 16:9.
  ```
- **영어로 쓴다.** 이미지 모델이 영어 지시를 훨씬 정확히 따른다.
- **글자를 넣지 않는다.** `no text, no letters`를 반드시 포함한다. 이미지 모델은 한글을 거의 항상 깨뜨린다. 구글 디스커버도 **글자가 많은 이미지와 로고 같은 범용 이미지는 썸네일로 잘 고르지 않는다** — 품질과 유입 양쪽에서 같은 결론이다. 문구가 필요하면 생성 후 편집기에서 얹는 것을 전제로 하고, 첫 줄 설명에 "생성 후 '4단계' 문구 삽입"처럼 적는다.
- **여섯 요소를 순서대로** 담는다: 대상 → 동작·구성 → 배경 → 색감·팔레트 → 조명 → 스타일. 마지막에 비율.
  스타일 자리에 기본으로 `photorealistic photograph`가 온다(위 첫 항목).
- **비율은 `16:9`를 기본**으로 한다. 구글 디스커버가 큰 썸네일로 띄우는 조건이 "너비 1200px 이상 + 16:9 가로"라, 발행 채널(Blogspot)에서 유입을 받으려면 가로 16:9여야 한다. 세로 컷이 꼭 필요하면 `3:4`, 표·카드뉴스형은 `1:1` — 다만 **세로·정사각은 디스커버 썸네일 후보에서 빠진다.** 이 표기는 실제 API 요청 크기로 변환된다(`16:9` → 1536x864, `4:3` → 1280x960). 적지 않으면 기본 16:9로 나간다.
- **글이 열릴 첫 이미지는 반드시 `16:9` 가로로 잡는다.** 디스커버·소셜 미리보기가 대표 이미지로 집어가는 자리라, 여기가 세로거나 정사각이면 큰 썸네일을 못 받는다.
- **한국 이야기면 한국 배경임을 명시한다.** `in Korea`, `Korean street`, `Korean police car`, `Korean cafe signage`처럼 국가·문화 맥락을 프롬프트에 직접 넣는다. 안 적으면 모델이 서구권 기본값으로 그린다 — 실측에서 한국 사건 기사에 **미국식 경찰차**와 **영어 간판의 서양 카페**(컵에 "SARAH L." 같은 영문 이름)가 생성됐다. 인물이 필요 없더라도 소품·간판·차량·거리 풍경이 한국이어야 본문과 따로 놀지 않는다.
- **실존 인물·브랜드·저작물을 생성하지 않는다.** 배우 얼굴, 드라마 스틸, 로고, 캐릭터는 AI로 만들지 말고 첫 줄을 `웹 검색`으로 바꾼다.
- **아이가 등장하는 이미지**는 실제 인물을 특정하지 않는 일반적 묘사로만 쓴다. 지아·지우를 프롬프트로 재현하려 하지 않는다.
- **프롬프트는 3~6줄로 쓴다.** 여섯 요소를 각각 한 줄 안팎으로 펼쳐 적고, 마지막 줄에 비율을 둔다. 한 줄로 몰아쓰면 결과가 스톡 이미지처럼 뻔해진다(실측 - 한 줄 프롬프트로 만든 커피컵·전화기 일러스트가 기사 맥락과 무관한 전형적 클립아트로 나왔다). 10줄이 넘으면 모델이 앞부분만 반영한다.

**웹 검색 프롬프트(검색어) 규칙**:

- **한국어로 쓴다.** 네이버·구글 이미지 검색창에 그대로 붙여넣을 검색어이지, 영어 지시문이 아니다.
- **고유명사를 구체적으로 넣는다.** 기관명·자료명·연도·인물명·행사명. "관련 사진"처럼 뭉뚱그리면 검색해도 원하는 결과가 안 나온다.
- **검색어는 한 줄, 8단어 안쪽으로 압축한다.** 검색창은 문장이 아니라 키워드 조합일 때 더 잘 맞는다.
- **가능하면 공공저작물·보도자료·공식 배포 이미지를 우선 찾도록 검색어를 구성한다**(기관명+"보도자료"/"배포"). 이런 소재는 첫 줄 설명에 "출처 표기 필요" 또는 "공식 배포 이미지만"이라고 반드시 덧붙인다.
- **라이선스 기준(2026-09-17 실측 후 정리).** 이 블로그는 광고가 붙는 **상업적 이용**이고 이미지를 크롭·리사이즈해서 쓴다. 그래서 "출처가 있다"와 "써도 된다"는 다르다.
  - 쓸 수 있는 것: 공공누리 1·2유형, CC BY, CC0·퍼블릭도메인, 정부·공공기관 보도자료와 배포 이미지, 기업 공식 홈페이지·보도자료·공식 SNS, **방송사·배급사가 홍보용으로 배포한 공식 포스터와 스틸컷**(사용자 결정 - 홍보 목적 배포물이고 출처를 밝히고 쓰는 것이 관행이다).
  - 쓸 수 없는 것: **CC의 NC(비영리)·ND(변경금지)**, 건축사무소·사진작가 등 개인·회사 **포트폴리오** 사진, **가격비교·쇼핑몰·오픈마켓이 재배포한** 상품 이미지, 뉴스사 자체 촬영 보도사진, 개인 블로그·커뮤니티 사진.
  - 실측 사례: 첫 수집 배치에서 경찰서 청사 사진이 `CC BY-NC-ND`인 채로 저장됐다 - NC는 광고 블로그에서 위반이고 ND는 크롭조차 막는다. `collectWebImages`가 이제 이 문자열을 보고 거부한다.
- **실존 인물·브랜드 로고·저작물**(배우 얼굴, 드라마 스틸, 로고, 캐릭터)은 공식 채널·보도자료를 특정하는 검색어로 쓴다(예: "OOO 공식 인스타그램", "OOO 방송사 보도자료").

### 8-3. 특정 일시의 회의·발표·의회 현장은 자리로 만들지 않는다 (2026-09-17)

**"9월 16일 국가정책조정회의 현장", "강릉시의회 조례안 부결 보도 사진", "9월 14일 국회 대정부질문
본회의장"처럼 특정 날짜·특정 회의를 찍은 사진을 이미지 자리로 잡지 않는다.** 그런 사진은 거의 항상
언론사가 촬영한 보도사진이고, 광고가 붙는 블로그에서는 쓸 수 없다. 정부가 직접 배포한 사진이 있는
경우도 있지만 특정 회의마다 있으리라 기대할 수 없다 - 실측에서 정책 기사 7자리가 이 이유로 전멸했다.

대신 **그 제도가 적용되는 일반적 현장**으로 바꾼다. 사건이 아니라 상태를 보여주는 자리로 만든다.

```
(X) [IMAGE: 9월 16일 국가정책조정회의에서 도입 방침을 보고하는 현장 사진 — 웹 검색]
(O) [IMAGE: 약을 처방받는 진료실에서 의사와 상담하는 환자 — AI 생성]

(X) [IMAGE: 강릉시의회 지원금 조례안 부결 관련 보도 사진 — 웹 검색]
(O) [IMAGE: 지역 주민센터 창구에서 지원금을 신청하는 시민 — AI 생성]
```

### 8-4. 웹 검색이냐 AI 생성이냐 — "실물을 특정해야 하는가"로 가른다 (2026-09-17)

획득 방식은 취향이 아니라 **그 자리가 실물 특정을 요구하는지**로 정해진다.

| 그 자리가 보여줘야 하는 것 | 획득 방식 | 예 |
|---|---|---|
| 특정 인물 | 웹 검색 | 류지현 감독, 특정 배우 |
| 특정 제품·작품 | 웹 검색 | 갤럭시 버즈4 케이스, 드라마 포스터 |
| 특정 장소의 실제 모습 | 웹 검색 | 오카자키 구장, 홍대 거리 |
| 제도·정책이 적용되는 **전형적 장면** | **AI 생성** | 주민센터 창구, 전통시장 결제, 병원 진료실 |
| 개념·절차·분위기 | **AI 생성** | 단계별 흐름, 사칭 문자를 확인하는 손 |

헷갈리면 이렇게 묻는다: **"이 자리에 다른 날 찍은 비슷한 사진을 넣어도 글이 성립하는가?"** 성립하면
일반적 장면이므로 AI 생성이다. 특정 그 사람·그 물건·그 장소여야만 하면 웹 검색이다.

### 획득 방식 배분

seo-guide는 직접 촬영·실사 이미지를 우대하고, AI 이미지로만 채운 글에 감점 신호가 있다고 본다. 그래서
2026-09-16까지는 "한 편에서 AI 생성은 절반까지"라는 상한을 뒀는데, **그 상한이 정책·지원금 기사에서
자리를 통째로 비우는 원인이 됐다**(실측: 민생지원금 4자리·임신중지약물 3자리 전부 미충족). 웹 검색으로
채울 수 있는 자리가 원래 적은 주제인데 비율을 맞추려다 빈 자리만 남은 것이다.

**그래서 비율 상한을 폐기하고 자리 성격으로 판단한다**(2026-09-17 사용자 결정). **빈 자리보다 AI
이미지가 낫다.**

- **실물 특정이 필요한 자리(§8-4 위쪽 세 줄)는 반드시 웹 검색.** 여기를 AI로 만들면 가짜가 된다.
- **나머지 자리는 AI 생성으로 채워도 된다.** 개수 제한 없음 - 정책·제도 기사는 AI가 다수가 되는 것이
  정상이다.
- 이미지 자리는 **정책 기사에서도 최소 5쌍**을 유지한다(사용자 결정 - SEO 기준을 맞춘다).
- 후기·리뷰·경험담 성격의 글은 실물이 주인공이므로 자연히 웹 검색 비중이 높아진다.
- 실존 인물·브랜드 로고·저작물은 AI로 만들지 않는다 — 반드시 웹 검색으로 돌린다.

---

## 9. 출력

### 경로

```
drafts/[키워드].md
```

파일명은 리서치 파일과 **같은 슬러그**를 쓴다(공백 → `-`, 특수문자 제거, 한글 유지). 이미 있으면 `drafts/[키워드]-2026-08-31.md`.

**파이프라인 헤드리스 실행에서는 이 규칙 대신 buildWritingPrompt.ts가 지정하는 절대 경로에
Write한다.** 이 §9는 인터랙티브 세션에서 이 문서를 직접 쓸 때만 적용된다.

### 파일 형식

```markdown
---
keyword: 아기 밤중수유 끊는 시기
research_file: research/아기-밤중수유-끊는-시기.md
skill_used: parenting-blog-writer
title: 아기 밤중수유 끊는 시기, 6개월설이 전부는 아니었어요
written_at: 2026-08-31
humanized: true
char_count: 2140          # 공백 제외
keyword_count: 4          # 본문 내 메인 키워드 등장 횟수
image_slots: 6            # [IMAGE:] + [IMAGE PROMPT:] 쌍의 개수
image_ai_generated: 2     # 그중 AI 생성으로 지정한 개수 (절반 이하)
hashtags: 10
sources_used: 9           # 실제로 원고에 반영한 리서치 출처 수
verdict_from_research: ok
---

(본문 — 네이버 블로그에 그대로 붙여넣을 수 있는 형태)

...

#해시태그1 #해시태그2 ... #해시태그10

<!-- 확인 필요
- 야간수유 중단 연령별 비율: 리서치에 통계 없어 해당 문장 생략함
-->

<!-- 사용한 출처
1. [official] 질병관리청 — https://...
2. [medical] 서울아산병원 — https://...
-->
```

본문에는 출처 URL을 노출하지 않는다(블로그 원고이므로). 대신 기관명은 본문에 쓰고, URL은 하단 주석에 남겨 검수 단계가 대조할 수 있게 한다.

---

## 10. 윤문 (필수 후처리)

초안을 다 쓴 뒤 **반드시** `/moai-writer:korean-humanize`를 실행한다. 생략 불가.

### 실행 방법

- 대상: 본문 전체 (frontmatter, HTML 주석, `[IMAGE PROMPT: ...]` 줄은 **제외**)
- `[IMAGE PROMPT: ...]`는 그대로 복사해 쓸 문자열(AI 생성 프롬프트의 영어든, 웹 검색의 한국어 검색어든)이라 윤문 대상이 아니다. 손대면 검색·생성이 안 되거나 결과가 바뀐다. 윤문 전에 따로 빼두고, 끝난 뒤 원래 자리에 그대로 되돌린다.
- 목적: 번역투, 기계적 병렬(첫째·둘째·셋째), AI 관용구(결론적으로·시사하는 바가 크다), 피동태 남용, 문두 접속사 남발 제거

### 윤문 후 반드시 재검증

윤문 스킬은 고유명사·수치·날짜·인용을 보존하도록 설계돼 있지만, **보존됐다고 가정하지 않는다.** 아래를 직접 대조한다.

- [ ] 모든 수치가 윤문 전과 동일한가
- [ ] 기관명·제도명·인물명이 그대로인가
- [ ] `[IMAGE: ...]` / `[IMAGE PROMPT: ...]` 마커가 전부 살아 있는가 (쌍 개수 일치)
- [ ] `[IMAGE PROMPT: ...]` 줄이 윤문 전과 완전히 동일한가 (AI 생성 프롬프트의 영어든, 웹 검색의 한국어 검색어든 손대지 않는다 — 윤문 대상이 아님)
- [ ] 해시태그 개수가 그대로인가
- [ ] 메인 키워드가 제목·첫 문단·소제목에 여전히 있는가
- [ ] 등급별 서술 강도가 유지됐는가 (community 근거가 단정형으로 바뀌지 않았는지)
- [ ] `#` 기호나 볼드 소제목이 새로 생기지 않았는가
- [ ] 헤지·책임 전가 문장이 새로 들어오지 않았는가 (`rules/facts-and-hedging.md` 금지 표현 재검사)
- [ ] 공백 제외 글자 수가 여전히 2,000~3,000자인가

윤문 결과가 원문과 30% 이상 달라졌다는 경고가 뜨면 **채택하지 말고** 원인을 확인한다. 문체 교정 범위를 넘어 내용이 바뀐 것이다.

윤문까지 마친 뒤 frontmatter의 `humanized: true`와 `char_count`를 최종값으로 갱신한다.
